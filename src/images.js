import React, { Component } from 'react'
import { Image } from 'react-native'

const images = () => (

        <Image source = {require('./images/singlePavarottiImage45Degrees.png')} />
    
)

export default images;